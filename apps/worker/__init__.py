"""ContractLens Worker — LangGraph workflow execution service.

Spec section 6: Executes the audit workflow as a stateful LangGraph graph
with checkpoint-based persistence and interrupt-safe human-in-the-loop.
"""
